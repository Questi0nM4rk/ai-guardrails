profile = "standard"

# Paths excluded from all linters in the check pipeline.
# Also propagated to biome.jsonc ignore, lefthook exclude, etc.
ignore_paths = ["tests/e2e/fixtures/**", ".venv/**", "node_modules/**", ".claude/**", "docs/plans/**"]
